package com.naver.b1.robot;

import static org.junit.jupiter.api.Assertions.*;

import javax.annotation.Resource;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.test.context.SpringBootTest;

import com.naver.head.Header;

@SpringBootTest
class ArmTest {
	
	/*
	 * @Resource(name = "leftArm") private Arm arm1;
	 * 
	 * @Resource(name = "rightArm") private Arm arm2;
	 */

	//@Autowired
	//@Qualifier("ra")
	//@Resource(name = "rightArm")
	//private Arm arm1;
	
	//@Autowired
	//@Qualifier("l")
	//@Resource(name = "leftArm")
	//private Arm arm2;
	/*
	 * @Autowired private Header header;
	 * 
	 * @Test void test() { assertNotNull(header); //assertNotNull(arm2); }
	 */
}
